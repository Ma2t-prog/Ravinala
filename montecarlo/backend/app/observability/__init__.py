"""
observability — Étape 11: Observabilité et opérabilité minimales.

Modules:
  logging_config  — Structured JSON logging with trace-id propagation
  metrics         — In-memory request/business metrics (Prometheus-compatible)
  health          — Deep health check per component
  alerts          — Two-tier alert / incident classification
  data_quality    — Data freshness and quality monitoring
"""
