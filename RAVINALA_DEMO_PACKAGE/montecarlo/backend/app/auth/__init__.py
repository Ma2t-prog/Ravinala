"""
auth — Étape 12: Sécurité et Gouvernance.

Modules:
  password   — Bcrypt hashing & verification
  jwt_handler— JWT token generation & validation
  rbac       — Role-based access control guards (FastAPI dependencies)
  audit      — AuditEvent logging
"""
